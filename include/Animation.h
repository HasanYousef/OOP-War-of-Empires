#pragma once
/*
#include <SFML/Graphics.hpp>

#include "macros.h"
#include "Textures.h"

class Animation {
public:
	static Animation& instance();
	void update(float);
	sf::IntRect get_rect(ObjectType) const;

private:
	Animation();

	int m_totalTime = 0,
		m_currentImage = 0;
};
*/